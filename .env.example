# Transkriptions-Provider: whisperx (Standard) oder elevenlabs
TRANSCRIPTION_PROVIDER=whisperx

# WhisperX Service URL (für lokale oder remote WhisperX-Instanz)
# Lokal: http://localhost:5000
# Remote: https://your-whisper-server.com
WHISPER_SERVICE_URL=http://localhost:5000

# WhisperX Modell (tiny, base, small, medium, large-v2, large-v3)
WHISPER_MODEL=large-v2

# ElevenLabs API Key (optional)
# - Wird verwendet wenn TRANSCRIPTION_PROVIDER=elevenlabs
# - Wird als Fallback verwendet wenn WhisperX nicht erreichbar ist
# - Wird für Voice Agent benötigt (wenn aktiviert)
ELEVENLABS_API_KEY=

# Voice Agent (optional, nur mit ElevenLabs)
NEXT_PUBLIC_ENABLE_AGENT=0
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=
